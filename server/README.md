# Paper 1.21.11 (local)

This folder is the **one shared world**. Do not replace it with a per-agent Minecraft install.

- Jar: `paper.jar` (Paper 1.21.11 build 132)
- Start: `start.bat` (`java -Xms4G -Xmx4G -jar paper.jar --nogui`)
- Address: `127.0.0.1:25565`
- `online-mode=false` for Mineflayer offline usernames
- EULA is already accepted locally

World, logs, cache, and the jar are gitignored. Do not delete the world.
